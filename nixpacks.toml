[phases.setup]
nixPkgs = ["nodejs_18", "npm-9_x"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npx prisma db push --skip-generate && HOSTNAME=0.0.0.0 PORT=${PORT:-3000} node .next/standalone/server.js"
