/**
 * Procedural spine act icons are composed as R3F meshes in ActIconMeshes.tsx
 * (box / capsule / sphere / cylinder / cone primitives — no GLTF).
 * This module exists as the stable import surface for icon geometry helpers.
 */
export { ActIconMeshes } from "./ActIconMeshes";
